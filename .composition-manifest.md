---
kind: composition_manifest
feature: review-widget
composed_at: 2026-05-10
last_re_emitted: 2026-05-15
composed_into: public/method/
status: stable
---

## 2026-05-15 — commentable-everything (direct-text + chromeAnchored) collapsed to config

Glinda's third in-flight variation collapsed into canonical via an upstream PR landing on 2026-05-15. Previously a local overhaul of `selectContentArea()` + `isInChrome()` + `tryAnchor()` + comment-write + comment-read filter, plus four new helpers (`hasDirectText`, `NEVER_ANCHOR`, `isInSiteChrome`, `CHROME_COUNTERS`); now two config flags in `public/method/contact-form.config.js`.

**Library files referenced (post-PR):**

- `library/features/review-widget/commentable-everything.md` (NEW atomic) — the canonical contract for both new options.
- `library/features/review-widget/anchor-strategy.md` — extended with §"Direct-text mode", §"Content area scoping" (conditional on chromeAnchored), §"Chrome anchoring" (promoted to full subsection).
- `library/features/review-widget/comment-lifecycle.md` — `page` field documented to accept `'__chrome__'` for site-chrome anchors.
- `library/features/review-widget/FEATURE.md` — `composes:` now lists 11 atomics; project-input table gains `Commentable content (optional)` and `Chrome anchored (optional)` rows; composition contract bullet extended.

**Changes in `public/method/review-mode.js`:**

- `selectContentArea()` now branches on `state.chromeAnchored`. `true` returns `document.body` (full page in scope). `false` returns `document.querySelector('main') || document.body` (canonical content-area selection with chrome auto-skipped by absence of `<main>` chrome containers).
- `tryAnchor()` rewritten to accept `state.commentableContent`-gated logic: `'direct-text'` mode applies `NEVER_ANCHOR` + `hasDirectText()`; `'allowlist'` mode applies `getEffectiveAnchorTags()` (ANCHOR_TAGS ∪ ANCHOR_TAGS_EXTRA) + `[data-comment-target]` opt-in.
- Chrome routing also gated: when `inSiteChrome === true` and `state.chromeAnchored === false`, returns early without anchoring. When `inSiteChrome === true` and `state.chromeAnchored === true`, assigns `chrome-{tag}-{n}` slug via `CHROME_COUNTERS`.
- `state.commentableContent` and `state.chromeAnchored` initialized in `init()` from `state.cfg.commentableContent` (defaults `'allowlist'`) and `state.cfg.chromeAnchored` (defaults `false`).
- All helpers (`hasDirectText`, `NEVER_ANCHOR`, `isInSiteChrome`, `CHROME_COUNTERS`) preserved in the file — they're always emitted (cheap, both modes share the JS).
- `anchorSubtree()` walk-all-descendants approach preserved; the mode-specific filter is inside `tryAnchor()`.
- Comment-write handler and comment-read filter unchanged — they already handle `'__chrome__'` correctly (was added in the 2026-05-14 local fork; now matches canonical).

**Changes in `public/method/contact-form.config.js`:**

- New top-of-config keys: `commentableContent: 'direct-text'` and `chromeAnchored: true`. Both annotated with provenance pointing at this re-emission. `ANCHOR_TAGS_EXTRA` retained (redundant under `'direct-text'` but useful if mode flips back to `'allowlist'`).

**Changes in `.claude/variations/review-widget.md`:**

- §"Behavioral variation — commentable everything (direct-text default + chrome-anchored)" collapsed to a one-paragraph note in the "Resolved" block. Variations file is now down to 170 lines (was 214 before the 2026-05-14 collapses).

**Verification (static):**

- `commentableContent: 'direct-text'` and `chromeAnchored: true` both present in `public/method/contact-form.config.js`.
- `state.commentableContent` and `state.chromeAnchored` both initialized from config in `init()`.
- `tryAnchor()` branches on `state.commentableContent` and `state.chromeAnchored`.
- `selectContentArea()` branches on `state.chromeAnchored`.
- §"Behavioral variation — commentable everything" no longer appears in variations file.

Browser-side verification still required after the next deploy — see the 5-step list at the top of this manifest.

## 2026-05-14 — anchor-extensibility + commentLifecycleMode + groupStatus bridge fix

Three improvements landed upstream as a single PR after the workbench validation pass on 2026-05-14. Glinda was the deployed evidence for all three (local forks of `ANCHOR_TAGS`, `renderComment()`/`renderGroup()`, and `groupStatus()`). The PR collapsed each fork into a canonical mechanism + config key; this re-emission applies the new specs.

**Library files referenced (post-PR):**

- `library/features/review-widget/anchor-strategy.md` — extended with §"Project-level extension (ANCHOR_TAGS_EXTRA)" and §"Per-element opt-in via [data-comment-target]".
- `library/features/review-widget/anchor-extensibility.md` (NEW atomic) — three extension mechanisms + MutationObserver + sticky-counter discipline.
- `library/features/review-widget/comment-lifecycle.md` — §"Vocabulary bridge" + §"Per-comment actions" with `'full'` / `'feedback-only'` modes.
- `library/features/review-widget/pending-archived-workflow.md` — §"Composition responsibilities" with `groupStatus()` returning filter-tab keys.
- `library/features/review-widget/FEATURE.md` — `composes:` now lists 10 atomics; project-input table gains `commentLifecycleMode` and `ANCHOR_TAGS_EXTRA` rows.

**Changes in `public/method/review-mode.js`:**

- `const ANCHOR_TAGS` reverted to canonical-only (removed the 5 glinda-added tags). Module-scoped `anchorCounters` introduced — sticky across init + observer passes.
- New helpers: `getEffectiveAnchorTags()`, `tryAnchor(el, tag)`, `anchorSubtree(rootNode)`. The new `anchorSubtree` runs both the tag-list pass AND the `[data-comment-target]` pass with dedupe.
- `anchorPage()` reduced to `anchorSubtree(selectContentArea())`.
- `setupDynamicAnchoring()` + `teardownDynamicAnchoring()` added. Observer scoped to the content area, listens `{ childList: true, subtree: true }`, calls `anchorSubtree(node)` per added element. Stored on `state.anchorObserver`.
- `pagehide` handler calls `teardownDynamicAnchoring()` before emitting `review.exited`.
- `state.lifecycleMode` initialized in `init()` from `state.cfg.commentLifecycleMode`.
- `renderComment()` rewritten: full mode emits Apply + Edit + Archive on pending, Restore + Archive on applied, Restore on archived (plus Delete in all cases); feedback-only emits Edit + Delete on pending, Restore + Delete on applied/archived.
- `renderGroup()` emits the bulk-archive footer when full mode AND group is active AND has pending comments.
- `groupStatus()` already returned `'active'` (from the 2026-05-13 local fix); now aligned with the canonical spec.

**Changes in `public/method/contact-form.config.js`:**

- `commentLifecycleMode: 'feedback-only'` and `ANCHOR_TAGS_EXTRA: ['label', 'blockquote', 'cite', 'figcaption', 'button']` added at top of the config object. Both annotated with provenance pointing at this re-emission.

**Changes in `.claude/variations/review-widget.md`:**

- §"Behavioral variation — feedback-only button set" and §"Behavioral variation — expanded anchor-tag list" collapsed to a single §"Resolved (canonical as of 2026-05-14)" block. Variations file shrank from 214 lines to 164 — no longer tracks code-fork details that are now spec-level.

Browser-side verification still required after the next deploy — load `?review=1` on an LP page, hover a form `<label>` → pill appears (ANCHOR_TAGS_EXTRA), append a `<p>` via DevTools → pill appears within the next render tick (MutationObserver), confirm sidebar default tab shows pending comments (groupStatus bridge), confirm row button set is Edit + Delete on pending (feedback-only mode).

## 2026-05-13 — inert-entry-button composed

Re-emitted `public/method/review-bootstrap.js` and `public/method/contact-form.config.js` to adopt the now-canonical `library/features/review-widget/inert-entry-button.md` atomic (landed upstream 2026-05-13). Glinda was the consumer that lacked the pattern; prota + odvaz had it from initial composition and the 2026-05-13 workbench validation pass surfaced the 2-of-3 convergence, pulling it into spec. Glinda's re-emission closes the gap.

**Upstream evidence consulted:**

- `library/features/review-widget/inert-entry-button.md` (NEW; last_updated 2026-05-13) — full contract: DOM shape, label resolution, inline style block, click handler, anti-patterns, variations expected.
- `library/features/review-widget/FEATURE.md` §"Composition contract" — relaxed inert-by-default qualifier carves out the one allowed inert-path element (the entry button); §"Lessons baked into composition" adds the inert-entry-button line; `feature_atomics:` list grew to nine.
- `library/components/toggle-button.md` — disambiguation note added: `.review-toggle` (sidebar-collapse, inside active chrome) vs `.review-toggle-btn` (inert-page entry, new atomic) — same shared label keys, different roles.
- `.claude/concepts/review-widget.md` §"Source of truth" — atomics list now nine.

**Reference compositions read in full:**

- `prota-studio/public/website/review-bootstrap.js` lines 31-95 (`injectKomentariButton`).
- `odvaz/public/susreti/review-bootstrap.js` lines 31-95 (byte-identical except for the config global name).

**Decisions locked in this re-emission:**

| Decision | Value | Rationale |
| --- | --- | --- |
| Visible label | `"Comments"` | Canonical English default; matches existing `sidebarTitle: "Comments"` for internal label consistency. |
| Tooltip | `"Open comment review mode"` | Canonical English default; same as prota's deployment. Operator picked the neutral / mechanical option (a) over Glinda-toned alternatives. |
| Glyph | None (text-only) | The atomic spec's `::before { content: "💬"; }` block is omitted entirely; `display: inline-flex` + `gap: 8px` retained as forward-compat. |
| Brand-token resolution | Glinda's purple palette pinned as literals in the inline `<style>` block | `primary-deep` → `#8705E4` (background), `primary-darker` → `#6804B5` (hover), `surface-base` → `#FFFFFF` (foreground); shadow tinted `rgba(1, 5, 94, …)` matching `surface-overlay` family. Do NOT carry odvaz's `#2c5e4a` / `#fbf8f3` forward. |
| Font stack | Library default for `font-stack-sans` | Glinda has no `brand/design-tokens/font-stack-sans.md` override; library default emitted inline. |

**Deliverable file changes:**

| Path | Lines before | Lines after | Delta |
| --- | --- | --- | --- |
| `public/method/review-bootstrap.js` | 122 | ~196 | +74 (new `injectEntryButton` function + DOMContentLoaded scheduler + refactored `reviewActive` hoisting + updated doc-comment) |
| `public/method/contact-form.config.js` | 70 | ~78 | +8 (new `toggleButton` / `toggleButtonTitle` keys + section comment) |

**Files NOT touched this pass:**

- `public/method/review-mode.css` — entry button has its own inline style block in the bootstrap; widget chrome CSS unchanged.
- `public/method/review-mode.js` — entry button is bootstrap-only; widget module unchanged.
- `.claude/variations/review-widget.md` — no new entry. The pattern is canonical; glinda is adopting, not diverging. This re-emission SHRINKS the canonical-vs-glinda gap.
- All other `public/method/**/index.html` pages — existing `<script src=".../review-bootstrap.js" defer>` wiring already loads the bootstrap on every page; the entry button surfaces everywhere automatically.

**Decision-log entry:** `.claude/decisions/2026-05-13-inert-entry-button-composed.md` (lands at commit time).

**Smoke-test checklist (post-deploy):**

1. Load `https://tnosugar.github.io/glinda-public-method/` with NO query parameter. Expect: small purple "Comments" button bottom-right; no banner, no sidebar, no widget chrome.
2. Click the button. Expect: URL gains `?review=1`; page hard-reloads.
3. After reload (now on `…?review=1`): expect full review chrome (banner top, sidebar right, content elements get `data-comment-id`). Expect: entry button is NOT visible (the `if (reviewActive) return;` early-exit fires).
4. Load `…/workshops/trial-method/` (sub-page; depth 2). Expect: same entry button bottom-right. Click → URL gains `?review=1`, reloads, full chrome activates.
5. View source on the inert page. Confirm: `<style>` block with `.review-toggle-btn` selector injected into `<head>`; `<button class="review-toggle-btn" data-review-skip="" type="button" title="Open comment review mode">Comments</button>` present at end of `<body>`.
6. Inspect the button. Confirm: background `#8705E4`, color `#FFFFFF`, hover background `#6804B5`. No emoji glyph before the label.
7. Print preview (Cmd-P). Confirm: button does NOT appear in print layout (`@media print { display: none; }` rule).

---

## 2026-05-13 — upstream alignment after library PR f40cbe7

Library PR `f40cbe7` landed in `tnosugar/organization-template`. The PR codifies the structural pattern that glinda re-emitted to on 2026-05-12b — class names (`.review-comment`, unprefixed inner-structure `.text` / `.replacement` / `.meta` / `.actions`, per-action suffix buttons `.apply-btn` / `.edit-btn` / `.archive-btn` / `.restore-btn` / `.delete-btn`), the three-state lifecycle (pending / applied / archived), the pill `circle-glyph` variant, state classes (`.has-comment` / `.has-applied-comment` / `.open` / `.active`), the `comment.applied` event, and the nine derived design tokens are now canonical upstream.

**No `review-mode.{css,js}` changes needed.** Deployed files already match the now-canonical pattern (they were emitted to it on 2026-05-12b). The drift this pass closes is in the spec/provenance layer, not the deployed layer.

**Variation file collapsed.** `.claude/variations/review-widget.md` shrunk from ~171 lines to 140. What's now canonical (collapsed out): all class-name renames, lifecycle, state classes, the `comment.applied` event, three-tab filter, pill geometry / circle-glyph variant, sidebar gutter strategy, banner gradient + `.dot` pulse, modal max-width + padding + backdrop blur, sage spotlight variant. What stays as variation: four hard divergences (banner `<a>` element + manual close, modal no-header-close, single center-bottom toast, button geometry 6px radius / 9px-18px padding / weight 500) plus brand-color overrides plus the nine derived-token overrides (now reframed — see below).

**Nine design-token files reframed.** `brand/design-tokens/{primary-darker,replacement-bg,replacement-text,surface-soft,surface-tint,surface-tint-2,rule,text-soft,text-faint}.md` updated:

- `status: widget-derived` → `status: stable`
- `last_updated: 2026-05-12` → `2026-05-13`
- `notes:` field rewritten from "widget-derived; introduced 2026-05-12b" framing to "Overrides library default `<hex>` for glinda's purple-tinted palette" framing. The widget composition surfaced the need for the token, but the value is now a stable project-wide override, not a one-off widget concern. The library now publishes its own defaults at these slugs (`surface-tint`, `surface-tint-2`, `rule`, `text-soft`, `text-faint`) and glinda overrides them; the four glinda-specific slugs (`primary-darker`, `replacement-bg`, `replacement-text`, `surface-soft`) remain as project-only additions.

**New atomic: `brand/design-tokens/spotlight-pulse.md`.** Per the "clean atomics" sign-off, the 1.5s spotlight-duration override (previously inline in `review-mode.css` only) is now formalized as a project-token file overriding `design.motion.spotlight-pulse` (library default 4s → glinda 1.5s, with `outline_solid_until` matching).

**Decision-log entry:** `.claude/decisions/2026-05-13-library-alignment-after-pr-f40cbe7.md`.

**Static verification (five-step §"Verifying isolation at composition time" from `css-isolation.md`):** unchanged from 2026-05-12b — no CSS/JS edits this pass. All five checks still pass (inert path, active path, hover pill, button isolation, has-comment outline).

---

## 2026-05-12b prota-alignment re-emission

Second re-emission today. The earlier 2026-05-12 pass applied the `css-isolation.md` contract literally; the result rendered but didn't match prota's deployed reference structurally. Operator surfaced the gap by pointing at `https://protastudios.ai/?review=1`. Fetching prota's CSS revealed that prota uses a different set of class names + state patterns than `css-isolation.md` lists — and prota is the deployed reference the spec was meant to codify. Glinda re-emits against prota's actual pattern.

Glinda keeps its brand colors (purple, indigo, cyan, coral, charcoal); prota's structural language (layout, class names, geometry, state-pattern vocabulary) is adopted.

**Divergences from `css-isolation.md` canonical recorded in `.claude/variations/review-widget.md`:**

- `.review-comment-row` → `.review-comment`
- `.review-row-text` / `.review-row-replacement` / `.review-row-meta` / `.review-row-actions` → unprefixed `.text` / `.replacement` / `.meta` / `.actions`
- `.review-row-action[data-variant="danger"]` → `.edit-btn` / `.delete-btn` / `.restore-btn` / `.apply-btn` (BEM suffixes)
- `.review-group-bulk-button` → `.archive-group-btn` inside `.review-group-footer`
- `.review-toggle` → `.review-sidebar-toggle` (vertical-text tab on right edge)
- `.review-pill-status[data-variant="..."]` → `.group-status.pending` / `.group-status.applied`
- `[data-has-comments="true"]` attribute → `.has-comment` state class
- New `.has-applied-comment` state class (sage outline for resolved comments)
- `data-open` attribute on sidebar → `.open` class (mobile-only)
- `aria-pressed` on filter tabs → `aria-pressed` + `.active` class
- Two-state lifecycle (pending/archived) → three-state lifecycle (pending/applied/archived)
- New event: `comment.applied` (payload: `{ id, pageSlug, anchorId, appliedAt }`)
- Three-tab filter (active / applied / archived) instead of two-tab
- Pill is a 22px circle with `+` glyph (was wide text pill "Add comment")
- Sidebar reserves 360px right gutter via `body { padding-right }` (was overlay)
- Banner uses gradient + `.dot` pulse indicator
- Modal max-width 560px (was 480px); padding 28px; backdrop-filter blur
- Spotlight duration 1.5s (was 4s); sage variant for applied elements
- Single center-bottom toast (was bottom-right stack)
- Button border-radius 6px / padding `9px 18px` / font-weight 500 (was 4px / `sp-sm sp-md` / 600)

**Library PR draft** at `~/work/projects/_pr-organization-template-prota-class-names.md` proposing the canonical spec adopt prota's deployed pattern. Once merged upstream and synced, most of the variation collapses into canonical.

**New tokens authored in `brand/design-tokens/`:**

| Token | Value | Role |
| --- | --- | --- |
| `primary-darker` | `#6804B5` | primary-button hover; banner gradient end |
| `replacement-bg` | `#FFF1EA` | suggested-replacement display background |
| `replacement-text` | `#5C2810` | suggested-replacement display foreground |
| `surface-soft` | `#FAFBFD` | sidebar-row hover bg |
| `surface-tint` | `#F2F4F9` | group-header bg; modal anchor-info bg |
| `surface-tint-2` | `#E0E5EE` | secondary-button bg; group separator |
| `rule` | `#C4CCDA` | structural divider color |
| `text-soft` | `#6B7280` | meta-row text |
| `text-faint` | `#97A0B1` | de-emphasized text |

**Decision-log entry:** `.claude/decisions/2026-05-12b-review-widget-prota-alignment.md`.

**Static verification (five-step §"Verifying isolation at composition time" from `css-isolation.md`):**

1. ✓ Inert path: every CSS rule scoped under `html[data-review-mode="on"]`.
2. ✓ Active path: bootstrap sets the attribute after CSS+module load.
3. ✓ Hover pill: `display: none` baseline + `:hover:not(:has(...))` override emitted (now with `:scope > .review-pill-container > .review-pill` deepest selector).
4. ✓ Button isolation: `.review-btn` carries `font-family: inherit`, `background: transparent`, `color: inherit`, explicit border / padding / cursor. BEM variants layer specific colors.
5. ✓ Has-comment outline: every `[data-comment-id]` outlined with `1px dashed var(--review-accent-warm-low)`; `.has-comment` deepens; `.has-applied-comment` shifts to cool.

Browser-side verification still required after the sync workflow updates `glinda-public-method`.

---

# review-widget composition manifest — public/method/

This sidecar records what was composed into `public/method/` and from where, so a future re-composition can reproduce or update the deliverables without guesswork, and so hand-edits become detectable.

**Do not hand-edit** the composed files (`review-bootstrap.js`, `review-mode.css`, `review-mode.js`) without recording the change in `.claude/variations/review-widget.md`. A re-composition without that record will silently clobber your edits.

## 2026-05-12 re-emission — css-isolation contract

Re-emitted `review-mode.css` and class references in `review-mode.js` against the new `library/features/review-widget/css-isolation.md` atomic (blob `368780bcf2892e7bffe4f7b74896278fbcc028f2`). Three real-world bugs the glinda adoption surfaced — host-page button styles cascading into widget buttons, missing dashed outline on has-comment elements, hover pill staying visible after cursor exit — drove the isolation contract upstream as canonical spec. The original 2026-05-10 composition predated the spec and emitted CSS without the isolation guardrails. This re-emission honors the contract:

- Class prefix changed from `.rv-` to canonical `.review-` (per `css-isolation.md` §"The contract" item 2). 50+ CSS rules renamed; 80+ JS class references renamed mechanically.
- BEM button variants — `.review-btn--primary`, `.review-btn--secondary`, `.review-btn--danger` — replacing the previous `data-variant` driven styling alone. Two JS button creations updated.
- Chrome reset block added near top of CSS — `all: revert; font-family: ...; box-sizing: border-box; line-height: 1.4;` on every chrome root (banner, sidebar, modal, modal-backdrop, toast, toast-stack, toggle, pill, pill-container, pill-status). Also `box-sizing: border-box` propagated to all descendants of chrome roots.
- Display-none baseline on `.review-pill` made explicit, non-overrideable by host page (per `pill-hover-deepest.md` §"Required emission"). Hover override uses high-specificity `[data-review-mode="on"] [data-comment-id]:hover:not(:has([data-comment-id]:hover)) > .review-pill-container > .review-pill { display: inline-flex; }`.
- Button isolation block emitted with explicit `font-family: inherit`, `background: transparent`, `color: inherit`, `border: 1px solid transparent`, `padding: var(...)`, `cursor: pointer` (per `components/button.md` §"Required emission"). Host-page `button { ... }` rules no longer cascade into widget buttons.
- Has-comment outline rule added: `outline: 1px dashed var(--review-accent-warm-low); outline-offset: 2px; position: relative;` on every `[data-comment-id]` element (per `anchor-strategy.md` §"Visual treatment").
- Container/pill `pointer-events` pair preserved (`pointer-events: none` on `.review-pill-container`, `pointer-events: auto` on `.review-pill`).
- Spotlight class renamed from `.rv-spotlight` to `.review-spotlit` (per `css-isolation.md` §"Spotlight class" naming).
- Pill host class renamed from `.rv-pill-host` to `.review-pill-container` (per `css-isolation.md` §"Required emissions" naming).
- Comment row class renamed from `.rv-row` to `.review-comment-row` (per `css-isolation.md` §"The contract" listing).
- CSS custom properties renamed from `--rv-*` to `--review-*` for consistency (internal scoping; not part of the isolation contract proper, but kept aligned).

Verified against `css-isolation.md` §"Verifying isolation at composition time" five-step check statically (browser-side verification still required on next deploy):

1. **Inert path.** No `<html data-review-mode="on">` set; CSS rules all gated under that selector; no rule fires when widget is off. ✓ (Static grep: zero unscoped widget rules.)
2. **Active path.** Bootstrap sets `<html data-review-mode="on">` after CSS + module load; chrome renders. ✓ (`review-bootstrap.js` line ~83 sets the attribute.)
3. **Hover pill behavior.** Default `display: none` on `.review-pill`; `:hover:not(:has(...))` override reveals only on deepest hovered anchored element; pill hides on cursor exit because the override stops matching. ✓ (CSS rules present.)
4. **Button isolation.** `.review-btn` carries `font-family: inherit`, `background: transparent`, `color: inherit`, explicit border + padding. Glinda's site-button cascade cannot win against the `[data-review-mode="on"] .review-btn` specificity floor. ✓ (CSS rule emitted in full.)
5. **Has-comment outline.** Every `[data-comment-id]` gets `outline: 1px dashed`; deepens to solid `accent-warm` on `[data-has-comments="true"]`. ✓ (CSS rule emitted; JS sets the `data-has-comments` attribute via `decorateHasComments()` after each RTDB update.)

Browser-side verification still required after the next push lands on the public mirror — load `?review=1`, hover an anchored paragraph, confirm exactly one pill appears, confirm a button in the composer modal looks like the widget variant (purple primary, indigo secondary, red danger) and not Glinda's site-button style.

## Composition source

| Layer | Path | Role |
| --- | --- | --- |
| Feature | `library/features/review-widget/FEATURE.md` | Composition spec; entry point |
| Concept | `.claude/concepts/review-widget.md` | Canonical pattern description |
| Architecture | `.claude/concepts/atomic-composition.md` | Three-layer overlay model |

## Library files referenced

All upstream files synced from `tnosugar/organization-template`. Last sync landed via commit `5886a1a — Sync from organization-template — atomic library + 5 new concepts`. Specific files consulted:

### Design tokens

| Path | Layer |
| --- | --- |
| `library/design/colors/primary-deep.md` | default token |
| `library/design/colors/accent-warm.md` | default token |
| `library/design/colors/accent-cool.md` | default token |
| `library/design/colors/feedback-amber.md` | default token |
| `library/design/colors/feedback-red.md` | default token |
| `library/design/colors/neutral-deep.md` | default token |
| `library/design/colors/surface-base.md` | default token |
| `library/design/colors/surface-overlay.md` | default token |
| `library/design/typography/font-stack-sans.md` | default token |
| `library/design/typography/size-scale.md` | default token |
| `library/design/spacing/scale.md` | default token |
| `library/design/motion/spotlight-pulse.md` | default token |
| `library/design/breakpoints/sidebar-collapse.md` | default token |
| `library/design/themes/default/*.md` | theme preset (pinned) |

### Components

| Path |
| --- |
| `library/components/banner.md` |
| `library/components/sidebar-drawer.md` |
| `library/components/toggle-button.md` |
| `library/components/pill.md` |
| `library/components/modal-shell.md` |
| `library/components/button.md` |
| `library/components/toast.md` |

### Feature atomics

| Path | Notes |
| --- | --- |
| `library/features/review-widget/anchor-strategy.md` | 2026-05-12 update added §"Visual treatment" with dashed-outline emission |
| `library/features/review-widget/comment-lifecycle.md` | |
| `library/features/review-widget/pending-archived-workflow.md` | |
| `library/features/review-widget/spotlight-on-click.md` | |
| `library/features/review-widget/pill-hover-deepest.md` | 2026-05-12 update added §"Required emission" with `display: none` baseline |
| `library/features/review-widget/firebase-rtdb-adapter.md` | |
| `library/features/review-widget/intl-plural-labels.md` | |
| `library/features/review-widget/css-isolation.md` | **NEW 2026-05-12** — isolation contract; blob `368780bcf2892e7bffe4f7b74896278fbcc028f2` |

### Events emitted

| Path | Emitted from |
| --- | --- |
| `library/events/review-entered.md` | init() after RTDB subscribe + anchor pass |
| `library/events/review-exited.md` | pagehide / programmatic teardown |
| `library/events/review-failed.md` | bootstrap error paths + firebase-init-error |
| `library/events/comment-created.md` | composer submit (new mode) |
| `library/events/comment-edited.md` | composer submit (edit mode) |
| `library/events/comment-archived.md` | row archive + bulk archive |
| `library/events/comment-restored.md` | row restore |
| `library/events/comment-deleted.md` | row delete after confirm |
| `library/events/spotlight-activated.md` | sidebar row click |

## Theme

**`theme: default`** (pinned via `brand/design-tokens/INDEX.md` frontmatter).

Rationale: every meaningful design token is overridden in `brand/design-tokens/` (Glinda's saturated jewel-tone palette doesn't match any of the three published theme presets). Pinning `default` rather than leaving theme unset gives traceability in the composition trail.

## Three-layer overlay trail (per `.claude/concepts/atomic-composition.md` §"How a project composes")

For each token: `final = project override (if present) ?? theme preset value ?? library default value`.

| Token | Library default | Theme `default` | Glinda override | Final |
| --- | --- | --- | --- | --- |
| `primary-deep` | `#1f3a5f` | `#1f3a5f` | `#8705E4` | `#8705E4` |
| `accent-warm` | `#d97706` | `#d97706` | `#FF8B5C` | `#FF8B5C` |
| `accent-cool` | `#16a34a` | `#16a34a` | `#53DEFF` | `#53DEFF` |
| `feedback-amber` | `#f59e0b` | `#f59e0b` | `#FFC857` | `#FFC857` |
| `feedback-red` | `#b91c1c` | `#b91c1c` | `#E63946` | `#E63946` |
| `neutral-deep` | `#1e293b` | `#1e293b` | `#01055E` | `#01055E` |
| `surface-base` | `#f8fafc` | `#f8fafc` | `#FFFFFF` | `#FFFFFF` |
| `surface-overlay` | `rgba(15,23,42,0.5)` | `rgba(15,23,42,0.5)` | `rgba(1,5,94,0.5)` | `rgba(1,5,94,0.5)` |
| `font-stack-sans` | (library default) | — | — | (library default) |
| `size-scale` | (library default) | — | — | (library default) |
| `spacing.scale` | (library default) | — | — | (library default) |
| `spotlight-pulse` | (library default; color references `accent-warm`) | — | — | (library default values, with accent-warm resolved to `#FF8B5C`) |
| `sidebar-collapse` | `800px` | — | — | `800px` |

Three tokens are widget-derived (introduced for this composition's needs, not part of Glinda's core brand four): `accent-warm`, `feedback-amber`, `feedback-red`. Per-token override files at `brand/design-tokens/{slug}.md` carry `status: widget-derived` so future readers know the provenance.

## Project inputs read

| Path | Role |
| --- | --- |
| `brand/design-tokens/INDEX.md` | Theme declaration (`theme: default`) + override index |
| `brand/design-tokens/primary-deep.md` | Token override `#8705E4` |
| `brand/design-tokens/accent-cool.md` | Token override `#53DEFF` |
| `brand/design-tokens/accent-warm.md` | Widget-derived `#FF8B5C` |
| `brand/design-tokens/feedback-amber.md` | Widget-derived `#FFC857` |
| `brand/design-tokens/feedback-red.md` | Widget-derived `#E63946` |
| `brand/design-tokens/neutral-deep.md` | Token override `#01055E` |
| `brand/design-tokens/surface-base.md` | Token override `#FFFFFF` |
| `brand/design-tokens/surface-overlay.md` | Token override `rgba(1,5,94,0.5)` |
| `public/method/contact-form.config.js` | `window.GLINDA_CONTACT_CONFIG` — FIREBASE_CONFIG (placeholders) + REVIEW_LABELS overrides |

## Per-project specifics resolved

| Field | Value | Source |
| --- | --- | --- |
| Slug target | `public/method/` | Only public slug |
| Config global | `GLINDA_CONTACT_CONFIG` | Convention `{PROJECT}_CONTACT_CONFIG` |
| RTDB path | `/comments/{pushId}` (flat) | `firebase-rtdb-adapter.md` |
| Page slug rule | Default per `anchor-strategy.md` (`/method/` → `method`; `/method/workshops/trial-method/` → `method-workshops-trial-method`) | Default |
| Content area | `<main>` if present, else `<body>` minus chrome | Default per `anchor-strategy.md` |
| Chrome anchoring | Excluded (default) | No project variation opt-in |
| Locale | `en` | Glinda config |
| Banner text | `"Reviewing the Glinda Method"` | Glinda config override |
| Footer signature | `"Kindness is Cooler than Coolness."` | Glinda config override per `.claude/variations/voice-md-shape.md` |

## Deliverables produced

| Path | Lines (2026-05-12) | Purpose |
| --- | --- | --- |
| `public/method/review-bootstrap.js` | ~95 | Inert-by-default loader; reads `?review=1`, dynamically loads CSS + module |
| `public/method/review-mode.css` | ~852 | Widget styles, scoped under `html[data-review-mode="on"]`; carries chrome reset + display:none pill baseline + button isolation block + has-comment outline + BEM button variants per `css-isolation.md`. Grew from ~510 lines in the 2026-05-10 emission. |
| `public/method/review-mode.js` | ~997 | ES module — the widget itself. Class names updated `rv-` → `review-` (80+ references); two button creations updated to BEM (`review-btn review-btn--primary`, `review-btn review-btn--secondary`); spotlight class renamed `.rv-spotlight` → `.review-spotlit`; pill-host renamed `.rv-pill-host` → `.review-pill-container`; comment row renamed `.rv-row` → `.review-comment-row`. |
| `public/method/.composition-manifest.md` | (this file) | Provenance record |
| `public/method/contact-form.config.js` | ~70 | (Project input, not strictly a deliverable, but shipped alongside the widget) |

## HTML script-tag wiring

Each page under `public/method/**/index.html` must include, in the `<head>`, BEFORE any other script:

```html
<!-- on /method/ (depth 0) -->
<script src="./contact-form.config.js"></script>
<script src="./review-bootstrap.js" defer></script>

<!-- on /method/workshops/trial-method/ (depth 2) -->
<script src="../../contact-form.config.js"></script>
<script src="../../review-bootstrap.js" defer></script>
```

The widget self-locates via `document.currentScript`, so deeper nesting picks up siblings correctly. The order matters: `contact-form.config.js` must be loaded BEFORE `review-bootstrap.js` so `window.GLINDA_CONTACT_CONFIG` is populated when the bootstrap checks it.

## Variations applied this composition

None. Glinda has no entry in `.claude/variations/review-widget.md` for the widget specifically; the composition uses library defaults plus standard token overrides. If a future deviation lands (e.g., chrome anchoring opt-in, no-bulk-archive variation), record it there and re-compose.

## Verification checklist

After re-compose or on first deploy, verify:

- [ ] Load `/method/` with NO query parameter. Confirm: no banner, no sidebar, no `review-mode.css` in the network panel, no `review-mode.js` in the network panel, no console errors.
- [ ] Load `/method/?review=1`. Confirm: banner appears, sidebar appears, content elements get `data-comment-id` attributes, hover on a paragraph reveals the "Add comment" pill.
- [ ] Click the pill. Confirm: composer modal opens; text-area focused; cancel + save buttons visible.
- [ ] Save a comment (with the placeholder Firebase config, this will FAIL with a toast — that's expected in degraded mode). Confirm: error toast appears with the `errorPrefix` message.
- [ ] Load `/method/workshops/trial-method/?review=1`. Confirm: widget activates here too; comments are scoped to the `method-workshops-trial-method` page slug.
- [ ] Resize viewport to <800px. Confirm: sidebar collapses; toggle button appears bottom-right.
- [ ] Once Firebase is provisioned and the placeholder config is replaced: full lifecycle (create → edit → archive → restore → delete) should round-trip via RTDB and the toast should land `saved` rather than `error`.

## Re-composition triggers

This composition will need a re-run when:

- Any file in `library/features/review-widget/`, `library/components/`, or `library/design/` (default tokens, themes/default/) changes upstream and the next `./work-setup/sync-conventions.sh` pulls it in.
- Glinda's `brand/design-tokens/` values change (e.g., the warm-accent question in `brand/BRANDBOOK.md` §7 resolves and a different value lands).
- Firebase config is provisioned and `contact-form.config.js` is updated with real values (this is a runtime change, not a composition change — but worth re-verifying everything still composes cleanly).
- A new feature is composed into `public/method/` (manifests stay per-feature; this file would not change but a sibling manifest would land).

## Related

- `.claude/concepts/atomic-composition.md` — the composition pattern
- `.claude/concepts/atomic-manifest-format.md` — manifest format spec
- `.claude/concepts/atomic-versioning.md` — versioning + pinning strategy
- `library/features/review-widget/FEATURE.md` — the feature spec
- `brand/design-tokens/INDEX.md` — the project's token override index
- `.claude/decisions/2026-05-10-adopt-review-widget.md` — the decision-log entry recording this adoption (lands at commit time)
